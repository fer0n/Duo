import SwiftUI

/// Colour used for cycling throughout the app, watch app and complications:
/// the accent colour darkened so it still reads as one family with steps.
let kmBarColor = Color.accentColor.mix(with: .black, by: 0.2)
